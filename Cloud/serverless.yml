service: auth-service

provider:
  name: aws
  runtime: dotnet6
  deploymentBucket:
    name: auth-service-deployment-bucket-prod

functions:
  registerUser:
    handler: Cloud.Services.AuthService::Cloud.Services.AuthService.RegisterUserLambda
    events:
      - http:
          path: register
          method: post
