service: auth-service

provider:
  name: aws
  runtime: dotnet6
  deploymentBucket:
    name: auth-service-deployment-bucket-${self:provider.stage}

functions:
  registerUser:
    handler: Cloud.Services.AuthService::Cloud.Services.AuthService.RegisterUserLambda
    events:
      - http:
          path: register
          method: post
